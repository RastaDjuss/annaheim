module.exports = {
  plugins: {
    'postcss-import': {}, // If you're using `import` syntax in CSS.
    tailwindcss: {},
    autoprefixer: {}, // Adds vendor prefixes for browser compatibility.
  },
};